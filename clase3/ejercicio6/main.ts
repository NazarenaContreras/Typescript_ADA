import { Geometry } from "./geometry";

const radio = 5;
const lado = 4;

console.log(`Area del circulo: ${Geometry.areaOfCircle(radio)}`);
console.log(`Area del cuadrado: ${Geometry.areaOfSquare(lado)}`);

